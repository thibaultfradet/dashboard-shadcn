import { FormTache } from "@/src/lib/components/personal/FormTache";

export default function addTache() {
  return (
    <div className="flex items-center justify-center">
      <FormTache projetId={null} />
    </div>
  );
}
